# Deployment Artifacts

Phase 1 includes minimal Docker and systemd foundations.

## systemd (example)

- `deploy/systemd/gst-automation-api.service`

Copy to `/etc/systemd/system/` and adjust paths/user.

